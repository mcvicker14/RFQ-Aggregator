# Used only by docker-compose.yml to set the Postgres container's password.
# Copy this file to .env (same folder as docker-compose.yml) and set a real value.
# This is separate from backend/.env, which the backend app itself reads.
POSTGRES_PASSWORD=CHANGE_ME_TO_A_LONG_RANDOM_STRING
