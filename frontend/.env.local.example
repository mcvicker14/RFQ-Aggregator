# Copy to .env.local for local development. Never commit the real .env.local file.
# URL of the FastAPI backend that /api/* requests are proxied to (see next.config.js).
BACKEND_URL=http://127.0.0.1:8000
